export type Category = {
    id: number,
    uid: string,
    domainEvents: [
    {}
  ],
    name: string,
    parentUid: string,
    parent: string,
    slug: string,
    isEnabled: boolean,
    isDeleted: boolean
  };

